services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      DB_PATH: /data/mr-stat.db
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - db-data:/data

volumes:
  db-data:
